[package]
name = "pool-timeline-model"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "AviUtl2式タイムラインの純粋データモデル（UI・GPU非依存）"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
