[package]
name = "pool-ffmpeg-io"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "ffmpeg 外部プロセスによる probe/書出し/取込（リンクなし・LGPL安全）"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
