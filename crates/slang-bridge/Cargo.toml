[package]
name = "pool-slang-bridge"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "slangc 呼び出し：Slang→SPIR-V/WGSL 変換＋キャッシュ"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
